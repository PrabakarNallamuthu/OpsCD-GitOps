apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Dev environment: inherits base policies.
# In dev, additional egress is allowed for debugging (e.g., pulling tools).
# Strict enforcement is applied only in prod.

resources:
  - ../../base/network-policies
  - allow-external-egress-dev.yaml
