/**
 * @opsera/shared — common types, DTOs, utilities, and constants
 * shared across all Opsera backend services.
 *
 * This package has zero internal dependencies on other @opsera/* packages
 * to prevent circular dependency chains (dependency DAG root).
 */

export * from './types/pagination.js';
export * from './types/result.js';
export * from './constants/index.js';
